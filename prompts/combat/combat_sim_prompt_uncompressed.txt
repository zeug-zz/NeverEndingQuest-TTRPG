Fix 1: prompts/combat/combat_validation_prompt_multipc_compressed.txt (Production)
1a. Clarify consolidation rule (line 143)
Current:
  consolidation_rule: "At most one updateEncounter per response when enemy state changes exist; consolidate ALL enemy changes into that action",
Proposed:
  consolidation_rule: "At most one updateEncounter per response when enemy state changes exist, consolidating ALL enemy STATE changes (HP/status/conditions of enemies only). Multiple updateCharacterInfo actions for different PCs/NPCs damaged during the same enemy phase is VALID and REQUIRED.",
1b. Add batch enemy phase routing example to @ROUTING_RULES (lines 152-154)
Current:
@ROUTING_RULES={
  valid_enemy_turn_pc_damage: "Enemy turn that only damages PCs/NPCs: updateEncounter may be housekeeping-only; PC/NPC HP MUST be in updateCharacterInfo."
}
Proposed:
@ROUTING_RULES={
  valid_enemy_turn_pc_damage: "Enemy turn that only damages PCs/NPCs: updateEncounter may be housekeeping-only; PC/NPC HP MUST be in updateCharacterInfo.",
  batch_enemy_phase: "When multiple enemies act in batch (after /end), expect: ONE updateEncounter consolidating all enemy actions/state + SEPARATE updateCharacterInfo for EACH PC/NPC that took damage. Multiple updateCharacterInfo is correct here, not a consolidation violation."
}
1c. Add explicit batch enemy phase example to @OUTPUT_EXAMPLE section (after line ~309, the consolidation_error example)
Insert after the consolidation_error example:
  batch_enemy_pc_damage: {
    "valid": true,
    "feedback": {
      "positive": "Batch enemy phase correctly processed. ONE updateEncounter for enemy housekeeping. Separate updateCharacterInfo for each damaged PC. Routing is correct.",
      "negative": "",
      "recommendation": ""
    },
    "note": "Example actions array: [updateEncounter(enemy attacks summary), updateCharacterInfo(PC1 takes X damage), updateCharacterInfo(PC2 takes Y damage)] -- this is VALID"
  }
1d. Add clarifying note to @CRITICAL_VIOLATIONS (line 177)
Current:
  multiple_update_encounter: "More than ONE updateEncounter action per response"
Proposed:
  multiple_update_encounter: "More than ONE updateEncounter action per response (NOTE: multiple updateCharacterInfo for different PCs/NPCs is VALID, not a violation)"
---
Fix 2: prompts/combat/combat_sim_prompt_multipc_compressed.txt (Production)
2a. Fix ambiguous plan_note (line 97)
Current:
  plan_note: "In plan field, explicitly state routing for each affected target: '[PC_NAME] takes 6 -> updateCharacterInfo', 'Enemy_X hits [PC_NAME] -> updateEncounter'"
Proposed:
  plan_note: "In plan field, explicitly state routing for each affected target: '[PC_NAME] takes 6 damage, HP Y->Z -> updateCharacterInfo'; 'Enemy_X attacks [PC_NAME] -> updateEncounter (enemy housekeeping only)'"
This matches line 75's clearer format and removes the ambiguity where "hits PC_NAME -> updateEncounter" could be misread as "PC damage goes in updateEncounter."
---
Fix 3: prompts/combat/combat_validation_prompt_multipc.txt (Uncompressed, human-readable mirror)
3a. Add clarification to the action usage section (around line 148)
Current (lines 147-150):
- FLAG AS INVALID: Any updateCharacterInfo targeting a creature with type: "enemy"
- FLAG AS INVALID: Any updateEncounter attempting to modify player or NPC HP
- FLAG AS INVALID: Mixed entity updates (e.g., one action trying to update both monster and player HP)
Proposed:
- FLAG AS INVALID: Any updateCharacterInfo targeting a creature with type: "enemy"
- FLAG AS INVALID: Any updateEncounter attempting to modify player or NPC HP
- FLAG AS INVALID: Mixed entity updates (e.g., one action trying to update both monster and player HP)
- FLAG AS VALID: Multiple updateCharacterInfo actions for different PCs/NPCs damaged during enemy batch phase. This is REQUIRED, not a consolidation violation. The consolidation rule applies ONLY to updateEncounter (one per response for all enemy state changes).
3b. Add batch enemy phase example to the COMBAT FLOW VIOLATION EXAMPLES section (after line ~301)
Insert after "VALID - Proper Flow" example:
VALID - Batch Enemy Phase with PC Damage:
Context: Enemy Phase after /end command. Goblin-1 hits Valor for 6, Goblin-2 hits Elara for 8.
Actions: [
  updateEncounter("Goblin-1 attacks Valor (hit). Goblin-2 attacks Elara (hit)."),
  updateCharacterInfo(characterName: "Valor", changes: "Takes 6 damage (HP 30->24)"),
  updateCharacterInfo(characterName: "Elara", changes: "Takes 8 damage (HP 22->14)")
]
Result: VALID. ONE updateEncounter for enemy housekeeping. Separate updateCharacterInfo for each damaged PC. Multiple updateCharacterInfo is correct and required here.
---
Summary
| File | Changes | Purpose |
|---|---|---|
| combat_validation_prompt_multipc_compressed.txt | 4 edits (1a-1d) | Stop validator from rejecting valid PC damage actions during enemy phase |
| combat_sim_prompt_multipc_compressed.txt | 1 edit (2a) | Remove ambiguous plan_note that could mislead combat LLM |
| combat_validation_prompt_multipc.txt | 2 edits (3a-3b) | Mirror compressed prompt for human readability |
The core fix is 1a - clarifying the consolidation rule to say "enemy STATE changes" and explicitly stating multiple updateCharacterInfo is VALID. The rest reinforces this from multiple angles to prevent the validator LLM from misinterpreting.
